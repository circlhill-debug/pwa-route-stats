{
  "name": "pwa-route-stats",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "esbuild src/app.js --bundle --format=iife --outfile=public/app.js --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "luxon": "^3.7.2",
    "vitest": "^4.0.3"
  }
}
